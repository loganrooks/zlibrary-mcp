---
phase: 18-v12-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - ISSUES.md
  - QUICKSTART.md
  - CONTRIBUTING.md
autonomous: true

must_haves:
  truths:
    - "CHANGELOG.md footer links use actual git tag names (v1.0, v1.1) not non-existent tags (v1.0.0, v1.1.0)"
    - "ISSUES.md shows ISSUE-DOCKER-001 as resolved with Phase 16-01 attribution"
    - "QUICKSTART.md no longer exists in the repository"
    - "CONTRIBUTING.md E2E section mentions Docker as a prerequisite"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Valid GitHub comparison URLs in footer"
      contains: "compare/v1.0...v1.1"
    - path: "ISSUES.md"
      provides: "ISSUE-DOCKER-001 in resolved section"
      contains: "ISSUE-DOCKER-001"
    - path: "CONTRIBUTING.md"
      provides: "Docker prerequisite note in E2E section"
      contains: "Docker"
  key_links:
    - from: "CHANGELOG.md"
      to: "GitHub tags (v1.0, v1.1)"
      via: "footer comparison URLs"
      pattern: "compare/v1\\.0\\.\\.\\.v1\\.1"
---

<objective>
Fix 4 documentation items from the v1.2 milestone audit: correct CHANGELOG footer links, mark ISSUE-DOCKER-001 resolved, delete superseded QUICKSTART.md, and add Docker prerequisite to CONTRIBUTING.md.

Purpose: Ensure documentation accurately reflects project state after v1.2 ship. Stale docs mislead contributors and users.

Output: 3 updated files (CHANGELOG.md, ISSUES.md, CONTRIBUTING.md) and 1 deleted file (QUICKSTART.md).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-v12-gap-closure/18-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CHANGELOG footer links and mark ISSUE-DOCKER-001 resolved</name>
  <files>
    CHANGELOG.md
    ISSUES.md
  </files>
  <action>
**CHANGELOG.md -- Fix footer comparison links (lines 101-104):**

Replace the 3 footer reference links at the bottom of CHANGELOG.md. Actual git tags are `v1.0` and `v1.1` (confirmed via `git tag -l`), not `v1.0.0` and `v1.1.0`.

Change:
```
[2.0.0]: https://github.com/loganrooks/zlibrary-mcp/compare/v1.1.0...v2.0.0
[1.1.0]: https://github.com/loganrooks/zlibrary-mcp/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/loganrooks/zlibrary-mcp/releases/tag/v1.0.0
```

To:
```
[2.0.0]: https://github.com/loganrooks/zlibrary-mcp/compare/v1.1...v2.0.0
[1.1.0]: https://github.com/loganrooks/zlibrary-mcp/compare/v1.0...v1.1
[1.0.0]: https://github.com/loganrooks/zlibrary-mcp/releases/tag/v1.0
```

Note: The `[Unreleased]` link (line 101) references `v2.0.0` which does not exist yet -- that is correct (it will resolve when the tag is created at release time). Leave line 101 unchanged.

**ISSUES.md -- Mark ISSUE-DOCKER-001 as resolved:**

Move ISSUE-DOCKER-001 (currently at lines 100-105 in "Open Issues" section) to the "Resolved Issues" section. Replace the existing entry with:

```markdown
### ISSUE-DOCKER-001: Docker numpy/Alpine Compilation [RESOLVED]
**Severity**: Was Low
**Resolution**: Phase 16-01 switched Docker base to `python:3.11-slim` (Debian) with pre-built wheels, eliminating Alpine numpy compilation failures.
```

Insert this after the last resolved issue entry (ISSUE-FN-001 through ISSUE-FN-004 block, before the `---` separator at line 36).

Also update ISSUES.md to mark ISSUE-GT-001 and ISSUE-PERF-001 as resolved (they will be fixed by Plan 01 which runs in the same wave). Move both from "Open Issues" to "Resolved Issues":

For ISSUE-GT-001:
```markdown
### ISSUE-GT-001: Footnote Tests Broken by Ground Truth v3 Migration [RESOLVED]
**Severity**: Was Medium
**Resolution**: Phase 18-01 updated test assertions in test_real_footnotes.py and test_inline_footnotes.py to use v3 schema accessors (`marker["symbol"]`, `definition["content"]`).
```

For ISSUE-PERF-001:
```markdown
### ISSUE-PERF-001: Performance Tests Flaky on CI Runners [RESOLVED]
**Severity**: Was Low
**Resolution**: Phase 18-01 loosened thresholds with 3x multiplier (0.005->0.015, 0.001->0.003, 0.010->0.030) to account for CI and dev machine variance while still catching algorithmic regressions.
```

Update the Executive Summary line to reflect that the test-full failures are resolved.
  </action>
  <verify>
Verify CHANGELOG links reference correct tags:
```bash
cd /home/rookslog/workspace/projects/zlibrary-mcp
grep -n "compare/v1\." CHANGELOG.md
grep -n "releases/tag/v1\." CHANGELOG.md
```

Expected: Links contain `v1.0` and `v1.1` (not `v1.0.0` or `v1.1.0`).

Verify ISSUE-DOCKER-001 is in resolved section:
```bash
grep -A2 "ISSUE-DOCKER-001" ISSUES.md
```

Expected: Shows `[RESOLVED]` with Phase 16-01 attribution.

Verify ISSUE-GT-001 and ISSUE-PERF-001 are in resolved section:
```bash
grep "ISSUE-GT-001\|ISSUE-PERF-001" ISSUES.md
```

Expected: Both show `[RESOLVED]`.
  </verify>
  <done>
CHANGELOG.md footer links use actual tag names (v1.0, v1.1). ISSUES.md shows ISSUE-DOCKER-001, ISSUE-GT-001, and ISSUE-PERF-001 as resolved with correct phase attribution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete QUICKSTART.md and add Docker prerequisite to CONTRIBUTING.md</name>
  <files>
    QUICKSTART.md
    CONTRIBUTING.md
  </files>
  <action>
**Delete QUICKSTART.md:**

```bash
git rm QUICKSTART.md
```

QUICKSTART.md is superseded by the README Quick Start section added in Phase 16-03. Research confirmed it is not referenced from any tracked file (README.md, CHANGELOG.md, package.json, or any other file).

**CONTRIBUTING.md -- Add Docker prerequisite to E2E section:**

The E2E testing section is at line 90. Currently reads:

```markdown
### End-to-End (Docker)

```bash
npm run test:e2e
```
```

Add a Docker prerequisite note between the heading and the code block. Updated section should read:

```markdown
### End-to-End (Docker)

**Prerequisites:** Docker must be installed and running. The E2E tests build and verify the Docker image.

```bash
npm run test:e2e
```
```
  </action>
  <verify>
Verify QUICKSTART.md is deleted:
```bash
cd /home/rookslog/workspace/projects/zlibrary-mcp
ls QUICKSTART.md 2>/dev/null && echo "STILL EXISTS" || echo "DELETED"
```

Expected: "DELETED"

Verify CONTRIBUTING.md has Docker prerequisite:
```bash
grep -A3 "End-to-End" CONTRIBUTING.md
```

Expected: Shows "Docker must be installed and running" between heading and code block.

Verify QUICKSTART.md is not referenced anywhere:
```bash
grep -r "QUICKSTART" --include="*.md" --include="*.json" --include="*.ts" . 2>/dev/null | grep -v ".planning/" | grep -v "node_modules/"
```

Expected: No results (or only the git rm output).
  </verify>
  <done>
QUICKSTART.md deleted from repository. CONTRIBUTING.md E2E section includes Docker prerequisite note.
  </done>
</task>

</tasks>

<verification>
Verify all 4 documentation items from the phase success criteria:

1. CHANGELOG links: `grep "compare/v1\.\|releases/tag/v1\." CHANGELOG.md` shows v1.0 and v1.1 (not v1.0.0/v1.1.0)
2. ISSUES.md: `grep "DOCKER-001.*RESOLVED" ISSUES.md` returns a match
3. QUICKSTART.md: `test -f QUICKSTART.md && echo EXISTS || echo GONE` returns GONE
4. CONTRIBUTING.md: `grep -A1 "End-to-End" CONTRIBUTING.md` shows Docker prerequisite

Check no markdown formatting issues:
```bash
head -5 CHANGELOG.md  # Verify file is not corrupted
head -5 ISSUES.md
head -5 CONTRIBUTING.md
```
</verification>

<success_criteria>
- CHANGELOG.md footer links resolve to valid GitHub URLs (v1.0 and v1.1 tags)
- ISSUES.md ISSUE-DOCKER-001 marked resolved with Phase 16-01 attribution
- ISSUES.md ISSUE-GT-001 and ISSUE-PERF-001 marked resolved with Phase 18-01 attribution
- QUICKSTART.md removed from repository (git rm)
- CONTRIBUTING.md E2E section notes Docker as a prerequisite
</success_criteria>

<output>
After completion, create `.planning/phases/18-v12-gap-closure/18-02-SUMMARY.md`
</output>
