---
phase: 15-cleanup-dx-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
user_setup: []

must_haves:
  truths:
    - "No blobs larger than 1MB exist in git history outside of LFS tracking"
    - "All current LFS pointer files remain intact and functional after filter-repo"
    - "The sample.pdf fixture file is tracked by LFS instead of as a raw blob"
  artifacts:
    - path: "(git history)"
      provides: "Clean git packfile without large non-LFS blobs"
  key_links:
    - from: "git filter-repo --strip-blobs-with-ids"
      to: "git lfs ls-files"
      via: "LFS pointers must survive blob purge"
      pattern: "git lfs ls-files.*pdf"
---

<objective>
Purge all remaining large binary blobs (>1MB) from git history via filter-repo, and migrate the one untracked large file to LFS.

Purpose: CLEAN-05 requires no large blobs in git history outside LFS. This is a one-way door (force-push) that must be done first and thoroughly to avoid additional history rewrites later. The credential scrub already rewrote history once; this batches all remaining cleanup into one final force-push.

Output: Clean git history (~107MB removed from packfile), sample.pdf migrated to LFS.
</objective>

<execution_context>
@/home/rookslog/.claude/get-shit-done-reflect/workflows/execute-plan.md
@/home/rookslog/.claude/get-shit-done-reflect/templates/summary-standard.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cleanup-dx-foundation/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Purge large blobs from git history and migrate sample.pdf to LFS</name>
  <files>(git history — no source files modified)</files>
  <action>
This task purges 7 specific blob SHAs from git history and migrates one current-tree file to LFS.

**Step 1: Pre-flight verification**
- Run `git lfs ls-files` and record the output (expect 5 PDF files tracked by LFS)
- Run `git stash` if there are any uncommitted changes
- Ensure git-filter-repo is available: `git filter-repo --version`

**Step 2: Migrate sample.pdf to LFS FIRST (before filter-repo rewrites history)**
- `git lfs track "__tests__/python/fixtures/rag_robustness/sample.pdf"`
- Verify `.gitattributes` updated
- `git add .gitattributes __tests__/python/fixtures/rag_robustness/sample.pdf`
- Commit: `chore(15-01): migrate sample.pdf to LFS tracking`

**Step 3: Run filter-repo to strip 7 old blob SHAs**
Create a temporary file with the blob SHAs to strip (these are OLD history-only blobs, not current LFS pointers):
```
9053b7405ece754dc52012b774a0a0c0fdd583a3
f274e990d307b47cef6985e7e7e2cc9c2a6707c1
bc9e3c167424010c523edb74ef8b1e961dc5ba8f
7eae90c48bcd35648bdc967b051ce0879033ff44
170b5a3d9f95b25e62e7df3a4a4bed740ae05c3b
f008f2389440939917a351bfe76fdbbe11363bed
54242e3d67c91dfa35d291148ac1b008b7769acc
```

Run: `git filter-repo --strip-blobs-with-ids /tmp/blobs-to-strip.txt --force`

**Step 4: Post-filter-repo verification**
- Run `git lfs ls-files` — must show same files as pre-flight (5 original + sample.pdf = 6 total)
- Run `git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectsize) %(objectname)' | awk '$1 == "blob" && $2 > 1048576' | sort -k2 -rn` — should show ZERO non-LFS blobs over 1MB
- Run `git gc --aggressive --prune=now` to reclaim space
- Verify repo size reduced significantly (was ~111MB packfile)

**Step 5: Re-add remote (filter-repo removes it)**
- `git remote add origin https://github.com/loganrooks/zlibrary-mcp.git`

**IMPORTANT:** Do NOT force-push yet. The checkpoint task handles that with user approval.
  </action>
  <verify>
- `git lfs ls-files` shows 6 LFS-tracked files (5 original + sample.pdf)
- `git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectsize) %(objectname)' | awk '$1 == "blob" && $2 > 1048576'` returns empty (no large non-LFS blobs)
- `du -sh .git/` shows significant reduction from previous size
  </verify>
  <done>All 7 target blob SHAs purged from history. sample.pdf migrated to LFS. No blobs >1MB exist outside LFS tracking. Remote re-added but not pushed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Approve force-push of cleaned history to GitHub</name>
  <files>(git remote)</files>
  <action>
Present verification results to user for approval before force-pushing rewritten history.

What was built: Git history has been rewritten to remove ~107MB of large binary blobs (old PDFs and ground truth text files). All current LFS pointers are intact. sample.pdf has been migrated to LFS. The remote has been re-added but nothing has been pushed yet.

How to verify:
1. Review the verification output from Task 1 (blob count, LFS file list, repo size)
2. Confirm you are ready for force-push to origin/master
3. Note: After force-push, any other clones will need `git fetch --all && git reset --hard origin/master`

When approved, Claude will run:
```
git push --force origin master
git push --force origin --tags
```

Resume signal: Type "approved" to force-push, or describe concerns.
  </action>
  <verify>
- `git push --force origin master` succeeds
- `git push --force origin --tags` succeeds
- `git log --oneline -3 origin/master` matches local master
  </verify>
  <done>Cleaned history force-pushed to GitHub. Remote and local are in sync.</done>
</task>

</tasks>

<verification>
- `git lfs ls-files` shows all expected LFS files including sample.pdf
- No blobs >1MB in git history outside LFS
- Remote is set and force-push completed (after checkpoint approval)
- `git log --oneline -5` shows intact recent history
</verification>

<success_criteria>
CLEAN-05 fully satisfied: no large binary blobs exist in git history outside LFS tracking. History rewritten with one force-push. sample.pdf now LFS-tracked.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cleanup-dx-foundation/15-01-SUMMARY.md`
</output>
