# Continue Here

**Paused:** 2026-02-11
**Milestone:** v1.2 Production Readiness
**Workflow step:** new-milestone → Step 8 (Research)

## Current Position

- Milestone v1.2 started, PROJECT.md and STATE.md updated and committed
- GSD Reflect v1.13.0 update committed (109 files, lint-staged fix included)
- Config migrated with health_check + devops sections
- Working tree is **clean** — no uncommitted changes

## What's Done

1. ✓ GSD Reflect upgrade (0.0.0 → 1.13.0) with migration log
2. ✓ lint-staged scoped to `src/` (was catching `.claude/*.js` and breaking pre-commit)
3. ✓ Milestone v1.2 scope defined through questioning
4. ✓ PROJECT.md updated with Active requirements for v1.2
5. ✓ STATE.md reset for new milestone
6. ✓ All committed — clean working tree

## What's Next

**Resume the new-milestone workflow at Step 8: Research**

User explicitly wants research before requirements. Spawn 4 parallel researchers:

### Milestone Scope (v1.2 — Production Readiness)

| Pillar | What |
|--------|------|
| Bug fixes | Failing paths.test.js, pytest collection errors, deprecated AsyncZlib cleanup |
| Test infrastructure | Reorganize tests, unified ground truth schema, proper labeling, expected output structure for full RAG pipeline |
| Structured RAG | Body + footnotes + metadata as separate linked files, corruption recovery working |
| RAG quality scoring | Automated precision/recall against ground truth, integrated into CI |
| Repo cleanup | Remove dead files, consolidate scattered docs, clean top-level |
| Documentation | API docs, usage examples, contributor guide, architecture diagrams |
| Packaging | README refresh, npm publish readiness, working CI pipeline |

### User Preferences (from questioning)

- **Repo cleanup scope:** Conservative — remove clearly dead files, keep historical docs
- **RAG output:** Fully structured — body + footnotes + metadata as separate linked files
- **Testing:** Both human-verified ground truth AND automated precision/recall in CI
- **Additional scope:** Fix known bugs + documentation overhaul (added beyond initial 3 pillars)

### Research Focus Areas

Research should focus on NEW capabilities only:
1. **Stack:** What tools/libs for RAG quality scoring, doc generation, npm packaging?
2. **Features:** What does "ideal structured RAG output" look like? Ground truth schema design?
3. **Architecture:** How does structured footnote output integrate with existing multi-file pipeline?
4. **Pitfalls:** Common mistakes in repo cleanup, test reorganization, npm publishing?

### Config

- Models: researcher=opus, synthesizer=sonnet, roadmapper=opus
- Mode: yolo, quality profile, research enabled

## Key Context

- RAG pipeline test on Derrida pages 120-125 showed footnote corruption recovery NOT activating (iii→* and t→† not recovered)
- Ground truth exists at `test_files/ground_truth/` but has 3 schema versions (v1, v2, v3) — needs unification
- 106 GSD files were blocking — now committed
- lint-staged `*.{ts,js}` was catching `.claude/` — fixed to `src/**/*.{ts,js}`

## Resume Command

```
/gsd:new-milestone
```

Then tell Claude: "Resume at Step 8 — research. Read .planning/.continue-here.md for context."

Or if you want to skip the full workflow and just run research:

```
Research the domain ecosystem for v1.2 Production Readiness — see .planning/.continue-here.md for scope.
```
